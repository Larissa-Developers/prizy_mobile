import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    marginTop: 30,
    alignSelf: 'center',
  },
  errorText: {
    marginTop: 20,
    fontSize: 20,
    alignSelf: 'center',
  },
});
